'use strict';

const SITE_LANGUAGES = ['auto', 'fr', 'en', 'es'];
const AI_LANGUAGES = ['auto', 'fr', 'en', 'es'];

const LANGUAGE_LABELS = {
  auto: 'Automatic',
  fr: 'French',
  en: 'English',
  es: 'Spanish',
};

module.exports = {
  SITE_LANGUAGES,
  AI_LANGUAGES,
  LANGUAGE_LABELS,
};
